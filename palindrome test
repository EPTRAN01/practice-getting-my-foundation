def input_user():
    x = int(input("x?: "))
    return x
def value_checker(x):
    array = list(str(x))
    length = len(array)
    mid_index = len(array) // 2
    if length % 2 == 0: #since it gives a remainder, if its zero it means its even and had a mid num
        first_half = array[:mid_index] #this gives number up to mid without the mid number
        second_half = array[:mid_index]
    else:
        break
        # For odd-length strings, skip the middle element
        first_half = array[:mid_index]
        second_half = array[mid_index + 1:]

    if first_half == second_half[::-1]:
        print("Value is a palindrome")
    else:
        print("Value is NOT a palindrome")
