
def input_user():
    city = input("please enter a city: ")
    month = input("Please enter a month: ")

def weather_temp(city, month):  #the whole function gets me the for loop that goes down each row, then if the condition is true it will print the value and close the file
    f = open("weather_temp(3).csv", "r") #reader
    lines = f.readlines()
    for line in lines:    #for loop for the whole list
        row = line.strip().split(",") #this gives me ['city', 'month', 'high', 'low']
        if row[0].lower == city.lower() and row[2].lower == month.lower()
            print(row[3]["high"])
            print(row[4]["low"])
        f.close()




